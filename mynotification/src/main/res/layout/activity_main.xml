<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/tb_one"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/design_default_color_error"
        app:logo="@drawable/ic_baseline_thumb_up_24"
        app:navigationIcon="@drawable/ic_baseline_west_24"
        app:title="自定义tollbar"
        app:titleTextColor="@color/design_default_color_secondary" />

    <androidx.appcompat.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_marginTop="20dp"
        android:background="@color/design_default_color_error"
        app:navigationIcon="@drawable/ic_baseline_west_24"
        app:titleTextColor="@color/design_default_color_secondary">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="自定义居中tollbar"
            android:textColor="@color/design_default_color_secondary"
            android:textSize="19sp" />
    </androidx.appcompat.widget.Toolbar>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <Button
            android:layout_width="150dp"
            android:layout_height="50dp"
            android:layout_marginBottom="20dp"
            android:onClick="fireNotification"
            android:text="发起通知" />

        <Button
            android:layout_width="150dp"
            android:layout_height="50dp"
            android:onClick="cancelNotification"
            android:text="取消通知" />
    </LinearLayout>
    >


</LinearLayout>